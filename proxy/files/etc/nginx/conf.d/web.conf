server {
  listen 80;
  listen [::]:80;

  server_name registry.*;

  location / {
      proxy_set_header Host $host;
      proxy_pass http://web/;
      proxy_buffering off;
      proxy_set_header X-Real-IP $remote_addr;
  }
}
